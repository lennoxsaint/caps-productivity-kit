version = 1
id = "caps-stable-update"
kind = "cron"
name = "CAPS stable update"
prompt = "Read .caps/automations/caps-update/prompt.md and execute it exactly. Apply only verified, compatible, non-disruptive stable updates; preserve local configuration, data, and overrides."
status = "PAUSED"
rrule = "FREQ=DAILY;BYHOUR=9;BYMINUTE=0;BYSECOND=0"
model = "gpt-5.6-luna"
reasoning_effort = "low"
execution_environment = "local"
target = { type = "project" }
